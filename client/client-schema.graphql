extend type Query {
  state: State!
}

extend type Mutation {
  addTask(input: AddTaskInput!): AddTaskPayload
}

input AddTaskInput {
  clientMutationId: String!
  text: String!
}

type AddTaskPayload {
  clientMutationId: String
  task: Task!
}

type State {
  tasks: [Task!]!
}